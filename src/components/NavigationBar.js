import { Container, Toolbar, Typography } from "@material-ui/core";

const NavigationBar = () => {
    return (  
        <Container>
            <Toolbar>
                <Typography>
                    INGSOC
                </Typography>
            </Toolbar>
        </Container>

    );
}

export default NavigationBar;